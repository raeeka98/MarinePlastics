import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

// ReactDOM puts the App component into the root div
ReactDOM.render(
  <App/>,
  document.getElementById('root')
);
